const ROOT_DIR = "/app";
const PRIVATE_DIR = `${ROOT_DIR}/private`;
const PUB_PATH = `${PRIVATE_DIR}/rsa.pub`;
const PRI_PATH = `${PRIVATE_DIR}/rsa.key`;
const JWT_PATH = `${PRIVATE_DIR}/jwt.key`;

export default {
  PRIVATE_DIR,
  PUB_PATH,
  PRI_PATH,
  JWT_PATH,
};
